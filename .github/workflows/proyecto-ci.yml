name: Build and Push Docker

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: |
          docker build -t mi-proyecto-docker:latest .

      # Opcional: Push to Docker Hub (requiere secrets DOCKER_USERNAME y DOCKER_PASSWORD)
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Push Docker image
      #   run: |
      #     docker tag mi-proyecto-docker:latest tuusuario/mi-proyecto-docker:latest
      #     docker push tuusuario/mi-proyecto-docker:latest